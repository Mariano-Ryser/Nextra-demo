# Lo que busque hace años.

## Component

{/* use States */}

import { useState } from 'react'

{/* Import CSS modules */}

import styles from '../components/counters.module.css'

export const Counter = () => {
  const [count, setCount] = useState(0);
  return (
    <div>
      <button onClick={() => setCount(count + 1)} className={styles.counter}>Clicked {count} times</button>
    </div>
  );
};


```js filename="codigo de siguiente componente" {3} copy
export const Counter = () => {
  const [count, setCount] = useState(0);
  return (
    <div>
      <button onClick={() => setCount(count + 1)} className={styles.counter}>Clicked {count} times</button>
    </div>
  );
};

<Counter/>
```

```js filename="Este componente lo escribimos en nuestro HTML despues de aberlo importado" {3} copy
<Counter/>
```

<Counter/>

## External Component

import Counters from '../components/counters'

<Counters />
